import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "חזרה על טעויות — תיאוריה",
};

export default function RetakeLayout({ children }: { children: React.ReactNode }) {
  return children;
}
